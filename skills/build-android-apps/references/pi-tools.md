# Pi Tool Mapping

Pi has no native skill tool: load a skill by reading its `SKILL.md` with the file-read tool when the skill applies. Track todos in `TODO.md` or plan file. Dispatch subagents inline when unavailable; never invent a `Task` call.
