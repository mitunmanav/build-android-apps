# Cursor Tool Mapping

Same surface as Claude Code. Invoke skills via `Skill` tool; if absent read `skills/<name>/SKILL.md`. SessionStart bootstrap arrives as `additional_context` (see `hooks/hooks-cursor.json`).
