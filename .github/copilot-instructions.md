# build-android-apps

Use the frontdoor skill `build-android-apps` for any Android app idea, build, crash, preview, or publish. Read `skills/build-android-apps/SKILL.md` when it applies. Map actions via `skills/build-android-apps/references/copilot-tools.md`. `state.json` is single source of truth; use `python -m state`.
