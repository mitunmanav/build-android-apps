# Copilot CLI Tool Mapping

Same surface as Claude Code. SessionStart bootstrap arrives as top-level `additionalContext`. Invoke skills via `Skill` tool; fallback is reading `skills/<name>/SKILL.md`.
