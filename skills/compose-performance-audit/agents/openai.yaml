interface:
  display_name: "Compose Performance Audit"
  short_description: "Audit recomposition, stability, and baseline profiles"
  default_prompt: "Use $compose-performance-audit to find why this screen recomposes on every state change."
