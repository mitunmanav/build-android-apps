---
description: List all commands in plain English — what each does.
allowed-tools:
  - Read
---

# /help

List all 32 commands in plain English.

## Context

- Working directory: !`pwd`

## Reporting Action

> [!IMPORTANT]
> Before invoking, say: "Here's what I can do — all commands in plain English."

## Your task

List every file in `commands/` with its `description` frontmatter. Group: core (`/setup`, `/make-app`, `/preview`, `/publish`), plan (`/add`, `/where`), ship (`/status`, `/screenshots`), dev (`/build`, `/lint`). Point to `SPEC.md` for spec. `$ARGUMENTS`
