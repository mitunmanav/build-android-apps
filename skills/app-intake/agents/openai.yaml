interface:
  display_name: App Intake
  short_description: Turn a vague app idea into a buildable spec
  default_prompt: Use $app-intake to translate my app idea into a spec
policy:
  allow_implicit_invocation: false
