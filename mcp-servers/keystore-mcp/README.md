# keystore-mcp

MCP server for Android upload keystore (generate, verify, rotate, backup, fingerprint) via `keytool`.

## Tools
- `generate`, `verify`, `rotate`, `backup`, `fingerprint`

## Install
```bash
pip install -e ./mcp-servers/keystore-mcp
```
